# Data Folder

Place the raw NOAA weather CSV here before running the pipeline.

Expected file name:

```text
saudi-hourly-weather-data_Historical.csv
```

The full dataset is intentionally not included in GitHub because it is large.
